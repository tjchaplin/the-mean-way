{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "test": "karma start --single-run --browsers PhantomJS"
  },
  "dependencies": {
    "express": "3.4.0",
    "jade": "*"
  }
}
